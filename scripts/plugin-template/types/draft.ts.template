// ---------------------------------------------------------------------------
// plugins/{{ALIAS}}/types/draft.ts — Draft payloads for create (AI / confirm flow)
//
// Align with what create{{PASCAL_ALIAS}}() expects on accept. Often matches
// Create{{PASCAL_ALIAS}}InputSchema.
// ---------------------------------------------------------------------------
import type { z } from 'zod';

import { Create{{PASCAL_ALIAS}}InputSchema } from './item';

export const Create{{PASCAL_ALIAS}}DraftSchema = Create{{PASCAL_ALIAS}}InputSchema;

export type Create{{PASCAL_ALIAS}}Draft = z.infer<
  typeof Create{{PASCAL_ALIAS}}DraftSchema
>;
