{
  "name": "nak-dm-bot",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "bot": "bun run index.ts",
    "watch": "bun run run-with-restart.ts",
    "lint": "bun run --cwd .. lint"
  },
  "dependencies": {
    "nostr-tools": "^2.23.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9"
  }
}
