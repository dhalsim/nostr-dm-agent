// ---------------------------------------------------------------------------
// src/prompt-session.ts — Interactive promptFn session contract
// ---------------------------------------------------------------------------

/** Resolved by pendingPrompt when the user sends !exit during an interactive prompt session. */
export const PROMPT_SESSION_EXIT = '__DM_BOT_PROMPT_EXIT__';
